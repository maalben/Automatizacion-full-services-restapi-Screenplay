#
# Esta sección define la configuración para diferentes ambientes.
#

environments {
    qa{
        base.url = "https://reqres.in"
    }
    dev{
            base.url = "https://reqres.in"
        }
}